import 'package:flutter/material.dart';

// Primary Color Palette
const Color primaryBlue = Color(0xFF00A8E8); // Bright Azure
const Color primaryBlack = Color(0xFF000000); // Pure Black
const Color richBlack = Color(0xFF1A1A1A);

// Slate Blue (Character Colors)
const Color slateBlue = Color(0xFF4A5568);
const Color blueGray = Color(0xFF718096);

// Secondary Color Palette
const Color lightBlue = Color(0xFFE6F7FF);
const Color mediumBlue = Color(0xFFB3E5FC);

// Neutral Colors
const Color pureWhite = Color(0xFFFFFFFF);
const Color offWhite = Color(0xFFFAFAFA);
const Color lightGray = Color(0xFFF5F5F5);
const Color mediumGray = Color(0xFFCCCCCC);
const Color darkGray = Color(0xFF666666);
const Color textGray = Color(0xFF333333);

// Accent Colors
const Color successGreen = Color(0xFF10B981);
const Color warningOrange = Color(0xFFF59E0B);
const Color errorRed = Color(0xFFEF4444);

// Interactive States
const Color primaryHover = Color(0xFF0084B4);
const Color primaryActive = Color(0xFF006B91);

// Typography Colors
const Color primaryHeadings = primaryBlack;
const Color secondaryHeadings = richBlack;
const Color bodyText = textGray;
const Color captions = mediumGray;
const Color links = primaryBlue;
const Color linkHover = primaryHover;

// Button Colors
const Color primaryButtonBackground = primaryBlue;
const Color primaryButtonText = pureWhite;
const Color primaryButtonHover = primaryHover;
const Color secondaryButtonBackground = pureWhite;
const Color secondaryButtonText = primaryBlue;
const Color secondaryButtonBorder = primaryBlue;

// Background Colors
const Color mainBackground = pureWhite;
const Color sectionBackgrounds = offWhite;
const Color cardBackgrounds = pureWhite;
const Color footerBackground = richBlack;
// Hero Section Gradient:
const LinearGradient heroGradient = LinearGradient(
  colors: [Color(0xFF00A8E8), Color(0xFF0084B4)],
);

// Illustration Colors
const Color characterSkin = Color(0xFFFDBCB4);
const Color characterHair = slateBlue;
const Color characterClothing = blueGray;
const Color foldedClothesShadow = Color(0xFFE2E8F0);
const Color decorativeElements = lightBlue; 